# AGENTS.md — obsidian-vault-llm

This is an Obsidian vault only. Markdown notes, no code. No build, lint, test, or package management.

- Do not introduce code, dependencies, or toolchain files unless explicitly requested.
- When editing notes: preserve YAML frontmatter keys, `[[wikilinks]]`, tags, and block IDs. Do not reformat unrelated notes.
- Do not rename or move note files without explicit request — renames break Obsidian links.
- Keep edits minimal and vault-native (plain Markdown, no HTML unless already used).
- There will be a AGENTS.md in each folder, read those for particular rules when you makes changes in that folder; Create them as needed when new folders are asked to be created.

